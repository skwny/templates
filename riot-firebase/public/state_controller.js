/* 

This controller manages ALL state changes for the app and generally functions in the following way:

	1. A piece of user logic triggers a state change. This can be simply a view change, or a store (model) change that influences the view change (e.g. a piece of displayed data changes).


*/

// Set router base
// riot.route.base('/');


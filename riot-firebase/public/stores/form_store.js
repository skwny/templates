/* RiotGear stuff */
function FormStore() {
	if (!(this instanceof FormStore)) return new FormStore()
	riot.observable(this)
  	var self = this
/* RiotGear stuff */

  	// self.on ...

  	// self.trigger ...

}